# Mail-in-the-Shell
MAIL IN THE SHELL is a local-first batch email tool with a browser UI and Python CLI for SMTP previews, controlled sends, resumable state, IMAP sent-copy support, and activity logging. It keeps credentials on your machine -supports optional macOS Keychain storage
